{
  "name": "pr-age-gate",
  "version": "0.2.3",
  "type": "module",
  "description": "Verify (and, as a GitHub Action, enforce) that a pull request has been open for a configurable minimum number of hours before it's considered mergeable. Public-repo checks work with no token at all. Plain TypeScript, no runtime dependencies: the Action runs the source directly via Node's type stripping, the npm package ships compiled JS.",
  "keywords": [
    "github",
    "github-actions",
    "pull-request",
    "ci"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=24.11.0"
  },
  "bin": {
    "pr-age-gate": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/check-pr-age.d.ts",
      "default": "./dist/check-pr-age.js"
    }
  },
  "files": [
    "dist",
    "action.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test __tests__/**/*.test.ts",
    "build": "tsc -p tsconfig.build.json",
    "prepack": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
